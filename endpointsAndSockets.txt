
Endpoints:
    agvs:
    GET /agvs (isOperator)
    GET /agvs/id (isOperator)
    PATCH /agvs/id/status (isOperator)
    POST /agvs (isAdmin)
    DELETE /agvs (isAdmin)

    jobs    
    GET /jobs (isOperator)
    GET /jobs/id (isOperator)
    DELETE /jobs/id (isAdmin)

    events
    GET /events (isOperator)

Sockets:
    from client to server:
    agv:goToStation
    job:advance
    job:create
    event:add

    from server to client:
    agv:update
    jobs:update
    events:new
    system:message
